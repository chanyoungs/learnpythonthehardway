x = raw_input("Think of any integer between 1 and 10")
x = raw_input("Multiply by 9...")
x = raw_input("Add the digits together. For example, if the number now is 37, do 3 + 7 = 10...")
x = raw_input("Subtract 4...")
x = raw_input("Think of the corresponding alphabet. i.e. 1=a, 2=b, 3=c and so on...")
x = raw_input("Think of an animal starting with that letter...")
x = raw_input("Are you thinking of...")

while True:
    print "ELEPHANT"

